spring:
  cloud:
    config: #关联config base文件 dev结尾
      name: base
      profile: dev
      label: master # 主干上的文件
      uri: http://127.0.0.1:12000
    refresh:
      extra-refreshable: none
  rabbitmq:
    host: 127.0.0.1

